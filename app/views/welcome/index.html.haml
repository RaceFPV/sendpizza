%row
  #headerwrap{name: "home"}
    %header.clearfix
      %h1 Gift of pizza
      %p Share pizza with anyone
      %a.btn.btn-default.btn-lg{href: "#learn"} Find Out More
  #greywrap
    .row
      .col-lg-4.callout
        %span.fa.fa-bolt
        %h2 Fast
        %p We take care of all the ordering hassle, so all you have to do is decide what kind of pizza you would like to share.
      .col-lg-4.callout
        %span.fa.fa-thumb-tack
        %h2 Safe
        %p No need to enter in an address, just ask for their giftofpizza username or email address and we'll make sure it gets there.
      .col-lg-4.callout
        %span.fa.fa-heart
        %h2 Love
        %p We love watching people give back, and we know once you try it you'll be hooked too.
  .container
    .row.mt
      #learn.col-lg-8.col-lg-offset-2{align: "center"}
        %h1 How it works
        %p Enter the name of the person you would like to send a pizza to
        .fa.fa-arrow-down
        %p Select the type of pizza you would like to send
        .fa.fa-arrow-down
        %p We'll take care of the ordering and pizza resturaunt selection process
        .fa.fa-arrow-down
        %p Have the gift of pizza on someones doorstep!
      .col-lg-4.col-lg-offset-4{align: "center"}
        %button.btn.btn-green{type: "button"} Get started now
  #services
    .container
      .row.mt
        .col-lg-1.centered
          %i.fa.fa-certificate
        .col-lg-3
          %h3 Quality Design
          %p It is a long established fact that a reader will be distracted by the readable content of a page when looking at its layout.
        .col-lg-1.centered
          %i.fa.fa-question-circle
        .col-lg-3
          %h3 Awesome Support
          %p It is a long established fact that a reader will be distracted by the readable content of a page when looking at its layout.
        .col-lg-1.centered
          %i.fa.fa-globe
        .col-lg-3
          %h3 Global Services
          %p It is a long established fact that a reader will be distracted by the readable content of a page when looking at its layout.
  #lg
    .container
      .row.centered
        .col-lg-2.col-lg-offset-1
          = image_tag 'pizza.jpg', style: 'width: 100px; height: 100px;'
        .col-lg-2
          = image_tag 'pizza.jpg', style: 'width: 100px; height: 100px;'
        .col-lg-2
          = image_tag 'pizza.jpg', style: 'width: 100px; height: 100px;'
        .col-lg-2
          = image_tag 'pizza.jpg', style: 'width: 100px; height: 100px;'
        .col-lg-2
          = image_tag 'pizza.jpg', style: 'width: 100px; height: 100px;'
  #call
    .container
      .row
        %h3 Need a pizza?
        .col-lg-8.col-lg-offset-2
          %p Click the button below to begin the process of asking for a pizza donation.
          %p
            %button.btn.btn-green.btn-lg{type: "button"} Request a slice
  :javascript
    $(function() {
        $('a[href*=#]:not([href=#])').click(function() {
            if (location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') || location.hostname == this.hostname) {

                var target = $(this.hash);
                target = target.length ? target : $('[name=' + this.hash.slice(1) + ']');
                if (target.length) {
                    $('html,body').animate({
                        scrollTop: target.offset().top
                    }, 1000);
                    return false;
                }
            }
        });
    });